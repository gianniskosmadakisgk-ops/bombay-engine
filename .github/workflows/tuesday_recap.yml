name: Tuesday Auto Recap
on:
  schedule:
    - cron: "0 9 * * TUE"  # Τρίτη 12:00 ώρα Ελλάδας (UTC+3)
  workflow_dispatch:

jobs:
  run-logger:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Bombay Chat
        run: |
          curl -X POST https://bombay-engine.onrender.com/tuesday_recap \
          -H "Content-Type: application/json" \
          -d '{"text": "📅 Tuesday recap triggered automatically by Bombay Engine."}'
