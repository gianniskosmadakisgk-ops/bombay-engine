name: Friday Auto Shortlist
on:
  schedule:
    - cron: "0 9 * * FRI"  # Παρασκευή 12:00 ώρα Ελλάδας (UTC+3)
  workflow_dispatch:

jobs:
  run-logger:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Bombay Chat
        run: |
          curl -X POST https://bombay-engine.onrender.com/friday_shortlist \
          -H "Content-Type: application/json" \
          -d '{"text": "📅 Friday shortlist triggered automatically by Bombay Engine."}'
